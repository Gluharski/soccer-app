export const startingXI = (data) => {
	console.log(data);

	// if (data.lenght > 0 && data !== undefined) {
	// 	// console.log(data.length);

	// 	console.log(data.lenght);
	// 	return data;
	// };

	// const homeTeam = data[0];
	// const awayTeam = data[1];

	// // let players = [];

	// // check if invalid data types
	// if (homeTeam == undefined && awayTeam == undefined) {
	// 	throw 'Invalid data!'
	// };

	// // // check if exists
	// if (homeTeam.startXI !== undefined) {
	// 	for (let info in homeTeam) {
	// 		console.log(info);
	// 	}
	// }
};
